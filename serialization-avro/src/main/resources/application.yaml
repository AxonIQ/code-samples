spring:
  docker:
    compose:
      enabled: true
      file: serialization-avro/docker-compose.yml

server:
  port: 8282

axon:
  converter:
    events: avro
    messages: avro
    general: jackson